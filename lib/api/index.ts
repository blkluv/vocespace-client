import { allRoomInfos, checkUsername, getUniqueUsername, historyRoomInfos, joinSpace } from './room';

export const api = {
  joinSpace,
  allRoomInfos,
  historyRoomInfos,
  getUniqueUsername,
  checkUsername
};
